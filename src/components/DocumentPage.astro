---
import DocumentLayout from '../layouts/DocumentLayout.astro';
import { getRouteDocument } from '../lib/route-documents.js';

const { routePath } = Astro.props;
const document = getRouteDocument(routePath);

if (!document) {
	throw new Error(`Missing route document for route: ${routePath}`);
}
---

<DocumentLayout document={document} />
