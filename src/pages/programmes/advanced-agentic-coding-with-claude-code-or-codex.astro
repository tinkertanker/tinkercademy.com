---
/**
 * Old slug — redirects to /programmes/agentic-engineering-with-claude-code-or-codex/.
 * `_redirects` handles this at the edge on Cloudflare; the emitted HTML is a
 * fallback for hosts that don't respect that file.
 */
return Astro.redirect('/programmes/agentic-engineering-with-claude-code-or-codex/', 301);
---
